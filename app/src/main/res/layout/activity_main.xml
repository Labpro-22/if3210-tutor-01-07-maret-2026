<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="06 — Contacts (ContentProvider)"
        android:textSize="18sp"
        android:textStyle="bold"
        android:padding="16dp"
        android:background="@color/purple_500"
        android:textColor="@color/white" />

    <!--
        RecyclerView: efficient scrollable list.
        - match_parent on both dimensions so it fills remaining space.
        - The LayoutManager (set in code) decides how items are arranged.
          LinearLayoutManager → vertical list (like ListView)
          GridLayoutManager   → grid
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="8dp" />

</LinearLayout>
